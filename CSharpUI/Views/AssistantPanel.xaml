<UserControl x:Class="ThreeDBuilder.Views.AssistantPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Width="300" Height="400"
             Background="Transparent">

    <Grid>
        <!-- Floating Assistant Window -->
        <Border Background="#1A1F2E" 
                BorderBrush="#3B82F6" 
                BorderThickness="2"
                CornerRadius="12"
                Padding="12"
                Effect="{StaticResource DropShadowEffect}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header with close button -->
                <Grid Grid.Row="0" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <materialDesign:PackIcon Kind="Robot" 
                                                Width="24" Height="24" 
                                                Foreground="#60A5FA"
                                                Margin="0,0,8,0"/>
                        <TextBlock Text="3D-Assistent"
                                   Foreground="{StaticResource TextPrimary}"
                                   FontSize="14"
                                   FontWeight="SemiBold"/>
                    </StackPanel>

                    <Button Grid.Column="1"
                            Command="{Binding CloseAssistantCommand}"
                            Style="{StaticResource MaterialDesignFlatButton}"
                            Foreground="{StaticResource TextSecondary}"
                            Padding="4"
                            Width="32" Height="32">
                        <materialDesign:PackIcon Kind="Close" Width="16" Height="16"/>
                    </Button>
                </Grid>

                <!-- Chat/Content Area -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,0,8">
                    <StackPanel>
                        <!-- Messages -->
                        <ItemsControl ItemsSource="{Binding AssistantMessages}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#0F1419"
                                            CornerRadius="6"
                                            Padding="8"
                                            Margin="0,4">
                                        <TextBlock Text="{Binding}"
                                                   Foreground="{StaticResource TextPrimary}"
                                                   FontSize="11"
                                                   TextWrapping="Wrap"/>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- Placeholder when no messages -->
                        <TextBlock Text="Hallo! Ich bin dein 3D-Assistent. Ich kann dir helfen, Formen zu erstellen, Tipps geben oder deine Fragen beantworten!"
                                   Foreground="{StaticResource TextSecondary}"
                                   FontSize="11"
                                   TextWrapping="Wrap"
                                   Margin="0,8"
                                   Visibility="{Binding AssistantMessages.Count, 
                                       Converter={StaticResource CountToVisibilityConverter}}"/>
                    </StackPanel>
                </ScrollViewer>

                <!-- Input Area -->
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0"
                             Text="{Binding AssistantInput, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="Beschreibe eine Form..."
                             Style="{StaticResource MaterialDesignTextBox}"
                             Foreground="{StaticResource TextPrimary}"
                             Background="#0F1419"
                             BorderBrush="#374151"
                             Padding="8,6"
                             Height="32"
                             VerticalContentAlignment="Center"/>

                    <Button Grid.Column="1"
                            Command="{Binding SendAssistantMessageCommand}"
                            Style="{StaticResource MaterialDesignFlatButton}"
                            Foreground="{StaticResource TextPrimary}"
                            Margin="4,0,0,0"
                            Padding="6"
                            Width="32" Height="32">
                        <materialDesign:PackIcon Kind="Send" Width="16" Height="16"/>
                    </Button>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
